% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.bbssr_samplesize.R
\name{plot.bbssr_samplesize}
\alias{plot.bbssr_samplesize}
\title{Plot the Exact Power Curve around a Sample Size Solution}
\usage{
\method{plot}{bbssr_samplesize}(
  x,
  N2.range = NULL,
  main = NULL,
  sub = NULL,
  xlab = NULL,
  ylab = NULL,
  ylim = NULL,
  ref.line = NULL,
  ref.line.N2 = NULL,
  show.points = TRUE,
  colours = NULL,
  base_size = 11,
  ...
)
}
\arguments{
\item{x}{An object of class \code{bbssr_samplesize} returned by
\code{\link{BinarySampleSize}}}

\item{N2.range}{Optional integer vector of sample sizes of group 2 at which the power is
evaluated. By default the selected sample size plus or minus five is used}

\item{main}{Title of the plot. The default of \code{NULL} builds the title from the
object and a single \code{NA} drops it}

\item{sub}{Subtitle of the plot, following the convention of \code{main}}

\item{xlab}{Label of the horizontal axis, following the convention of \code{main}}

\item{ylab}{Label of the vertical axis, following the convention of \code{main}}

\item{ylim}{Numeric vector of length two giving the range of the vertical axis, or
\code{NULL} for a range chosen from the data}

\item{ref.line}{Numeric vector of heights at which a dashed horizontal line is drawn.
The default of \code{NULL} places one line at the target power and a single \code{NA}
draws no line}

\item{ref.line.N2}{Numeric vector of positions at which a dotted vertical line is drawn.
The default of \code{NULL} places one line at the selected sample size and a single
\code{NA} draws no line}

\item{show.points}{Logical. If \code{TRUE}, the default, a marker is drawn at every
evaluated sample size. Set it to \code{FALSE} to draw the curve alone, which is easier
to read over a wide range}

\item{colours}{Colour of the curve}

\item{base_size}{Base font size of the theme, in points}

\item{...}{Further arguments, currently ignored}
}
\value{
A \code{ggplot} object
}
\description{
Recomputes the exact power over a range of sample sizes of group 2 and marks the
selected sample size and the target power.
}
\details{
The power is recomputed at every point of \code{N2.range}, so a wide range combined with
one of the unconditional tests can take a long time to evaluate.

The vertical range is imposed with \code{\link[ggplot2]{coord_cartesian}}, so points
outside \code{ylim} are hidden rather than removed from the data.
}
\examples{
\donttest{
ss <- BinarySampleSize(p1 = 0.4, p2 = 0.2, r = 1, alpha = 0.025,
                       tar.power = 0.8, Test = 'Chisq')
plot(ss)

# Enlarge the type, rename the axes and draw neither reference line
plot(ss, base_size = 14, ref.line = NA, ref.line.N2 = NA,
     xlab = 'Sample size of the control group', ylab = 'Exact power')
}

}
